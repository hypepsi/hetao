# HetaoLog - 宝宝记录工具

极简主义的宝宝记录应用，专注于核心数据的实时展示和快速记录。

## 功能特性

- 📱 **极简大卡片设计** - 屏幕中央展示3个核心卡片
- 🍼 **喂奶记录** - 实时显示距离上次喂奶时间，今日次数和累计奶量
- 💩 **排便记录** - 记录大小便次数和详细信息
- 📊 **历史记录** - 查看、编辑、删除历史记录
- 🔐 **简单认证** - 密码登录，30天免登

## 技术栈

- **Framework**: Next.js 14+ (App Router)
- **UI**: shadcn/ui (Card, Dialog, Sheet, Form, Badge, Button)
- **Style**: Tailwind CSS (Zinc 色系)
- **DB**: SQLite + Prisma
- **State**: SWR (自动刷新数据)
- **Time**: date-fns (时间计算)

## 部署信息

- **项目路径**: `/root/hetalog`
- **端口**: 3000
- **进程管理**: PM2
- **数据库**: SQLite (`prisma/dev.db`)

## 管理命令

```bash
# 查看应用状态
pm2 status

# 查看日志
pm2 logs hetalog

# 重启应用
pm2 restart hetalog

# 停止应用
pm2 stop hetalog

# 启动应用
pm2 start hetalog
```

## 访问应用

- **本地访问**: http://localhost:3000
- **登录密码**: `hetao@sbl`

## 端口配置

确保服务器防火墙开放 3000 端口：

```bash
# Ubuntu/Debian
sudo ufw allow 3000/tcp

# 或使用 iptables
sudo iptables -A INPUT -p tcp --dport 3000 -j ACCEPT
```

## 数据库管理

```bash
# 查看数据库
npx prisma studio

# 重置数据库（谨慎使用）
npx prisma db push --force-reset
```

## 开发模式

```bash
# 安装依赖
npm install

# 初始化数据库
npx prisma db push

# 启动开发服务器
npm run dev
```






